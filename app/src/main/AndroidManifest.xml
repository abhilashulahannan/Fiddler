<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fiddler">

    <!-- Dangerous permissions (require runtime approval) -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- take pictures and videos -->
    <uses-permission android:name="android.permission.READ_CONTACTS" /> <!-- read your contacts -->
    <uses-permission android:name="android.permission.CALL_PHONE" /> <!-- directly call phone numbers -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- nearby devices -->

    <!-- Bluetooth (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Storage permissions for rotating ringtones -->
    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Android <=12 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Optional: allow changing system settings (for automatic ringtone) -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Normal permissions (granted automatically) -->
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- control vibration -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- have full network access -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- view network connections -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- prevent phone from sleeping -->
    <uses-permission android:name="android.permission.REORDER_TASKS" /> <!-- re-order running apps -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- run foreground service -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" /> <!-- access Do Not Disturb -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" /> <!-- ask to ignore battery optimisations -->
    <uses-permission android:name="com.android.vending.BILLING" /> <!-- Google Play billing service -->
    <uses-permission android:name="com.android.vending.CHECK_LICENSE" /> <!-- Google Play license check -->

    <!-- For querying installed packages (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Required for overlay/status-bar style views -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Play Install Referrer API -->
    <queries>
        <intent>
            <action android:name="com.android.vending.INSTALL_REFERRER_SERVICE" />
        </intent>
    </queries>

    <!-- Legacy external storage support for Android 10 -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.Fiddler">

        <!-- Splash screen is the launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Fiddler">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Permissions screen -->
        <activity
            android:name=".PermissionsActivity"
            android:exported="false" />

        <!-- Main app screen -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>
</manifest>
